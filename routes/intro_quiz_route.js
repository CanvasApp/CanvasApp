'use strict';

module.exports = function(app) {
  var Quiz = require('../models/quiz_model');
  var Intro = require('../models/quiz_intro_model.js');

  app.get('/api/introquiz', function(req, res) {
    
  })  
}
